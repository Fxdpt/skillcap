{% extends "base.html.twig" %}

{% block body %}
	<div class="row mt-2">
		<small>
			<a href="{{ path('type_show',{"id":task.skill.type.id}) }}">{{ task.skill.type.name }}</a>
			>
            <a href="{{ path('skill_show',{"id":task.skill.id}) }}">{{ task.skill.name }}</a>
			

		</small>
	</div>
	<div class="row mt-3">
		<h1>{{ task.name }}</h1>
	</div>
	<div class="row">
		<article>{{ task.description | raw }}</article>
	</div>


{% endblock %}
